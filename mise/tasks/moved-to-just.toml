# Moved to just, kept here to preserve backwards compatibility.

[build-styles]
description = "$just build-styles"
run = ["just build-styles"]

[build-server]
description = "$just build-server"
run = ["just build-server"]

[build-client]
description = "$just build-client"
run = ["just build-client"]

[build-server-release]
description = "$just build-server-release"
run = ["just build-server-release"]

[clean-all]
description = "$just clean-all"
run = [
"just clean-all"
]

[prefetch-gleam-deps]
description = "$just prefetch-gleam-deps"
hide = true
run = "prefetch-gleam-deps"

[create-data-dirs]
hide = true
run = ["just create-data-dirs"]

[bun-install]
description = "$just bun-install"
run = "just bun-install"
